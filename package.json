{
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "jest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "setup": "node setup.js",
    "deploy": "npm run lint && npm run test && npm start"
  }
}
